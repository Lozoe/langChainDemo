# 📚 RAG 技术与应用笔记

> **RAG** (Retrieval-Augmented Generation)：检索增强生成，结合信息检索与文本生成，提升大模型回答的**准确性**、**时效性**与**专业性**。

## 🎯 核心目标
- **解决知识时效性问题**：LLM 训练数据静态，RAG 可接入最新外部知识。
- **减少模型幻觉**：基于真实检索结果生成，降低“胡说”概率。
- **提升专业领域回答质量**：融合垂直领域私有知识库。

## 🔍 RAG vs 其他模式

| 方法 | 适用场景 | 优势 | 劣势 |
| :--- | :--- | :--- | :--- |
| **提示工程 (Prompt Engineering)** | 简单任务、无需新知识 | 快速、低成本 | 能力有限，无法处理复杂或未知知识 |
| **RAG** | 需要**外部/私有/最新**知识 | 知识可更新、可解释、成本低 | 依赖检索质量，对文档预处理要求高 |
| **微调 (Fine-tuning)** | 需要改变模型**行为/风格/能力** | 深度定制，效果稳定 | 成本高、周期长、知识难更新 |

> 💡 **思考**：当问题超出 LLM 的**背景知识**或需要**最新信息**时，优先选择 RAG。

---

## 🧩 RAG 核心原理与流程

RAG 的工作流程可分为三个核心阶段：

```mermaid
flowchart LR
    A[用户提问\nUser Query] --> B[检索 Retrieval]
    subgraph “知识库构建”
        C[原始文档\nDocuments] --> D[文档分块\nChunking]
        D --> E[向量化\nEmbedding]
        E --> F[向量数据库\nVector DB]
    end
    B -->|查询向量| F
    F -->|Top-K 相关片段| G[增强上下文\nAugmented Context]
    G --> H[生成 Generation]
    H --> I[最终答案\nFinal Answer]