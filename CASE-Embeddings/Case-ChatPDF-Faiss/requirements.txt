# ============================================
# ChatPDF-Faiss 项目依赖列表
# Python >= 3.10
# ============================================

# PDF 读取（PyPDF2 已停止维护，最终版本 3.0.1）
PyPDF2==3.0.1

# LangChain 核心框架（1.x 系列，与环境中的 langchain-classic / langgraph-prebuilt 兼容）
langchain==1.2.7
langchain-core==1.2.7
langchain-community==0.4.1

# 阿里云 DashScope SDK（通义千问 LLM + Embedding）
dashscope==1.25.12

# FAISS 向量数据库（CPU 版本）
faiss-cpu==1.13.2

# 环境变量加载
python-dotenv==1.2.1

# Tokenizer（LangChain 运行时依赖）
tiktoken==0.12.0
